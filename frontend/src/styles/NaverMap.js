import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%',
    flex: 1,
    // borderWidth: 1,
  },
  mapContainer: {
    flex: 1,
  },
});
export default styles;
