import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  bottomSheetTitle: {
    color: '#434343',
    fontSize: 18,
    fontWeight: '600',
    // borderWidth: 1,
  },
});

export default styles;
